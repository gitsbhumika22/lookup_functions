# Excel Lookup Functions Practice

## 📌 Project Overview
This project demonstrates the use of **lookup functions in Microsoft Excel**, specifically **VLOOKUP (Vertical Lookup)** and **HLOOKUP (Horizontal Lookup)**.  
These functions are used to search for data in tables and return corresponding values from different rows or columns.

---

## 🧾 Dataset Details
- Type: Dummy dataset for practice
- Organized in tabular format
- Data arranged both **vertically** and **horizontally** for lookup operations

---

## 🛠 Tools Used
- Google sheets

---

## 🔧 Excel Lookup Functions Used

### 1️⃣ VLOOKUP (Vertical Lookup)
- Searches for a value in the **first column** of a table
- Returns a value from the **same row** in a specified column
- Used when data is arranged **vertically**

**Example:**
=VLOOKUP(A2, A2:D20, 3, FALSE)

---

### 2️⃣ HLOOKUP (Horizontal Lookup)
- Searches for a value in the **first row** of a table
- Returns a value from the **same column** in a specified row
- Used when data is arranged **horizontally**

**Example:**
=HLOOKUP(A1, A1:D10, 2, FALSE)

---

## 🔍 Steps Performed

1. Created lookup tables in Excel
2. Applied **VLOOKUP** to fetch data from vertical tables
3. Applied **HLOOKUP** to fetch data from horizontal tables
4. Verified results for exact matches
5. Validated accuracy of returned values

---

## 📁 Project Files
- `lookup_functions.xlsx` – Excel file with VLOOKUP and HLOOKUP examples
- `README.md` – Project documentation

---

## 🎯 Learning Outcomes
- Understood the difference between vertical and horizontal lookups
- Learned how to retrieve related data efficiently
- Gained practical experience with Excel lookup functions

---

## 📌 Author
**Bhumika Bhoir**  
